// Package signing is the driven adapter that implements the core's Signer and
// KeyStore ports with real key-bearing crypto (RSA/EC signing, JWKS). It is
// empty in the skeleton slice; the signing implementation lands in Slice 1.
package signing
