// Package controlapi is the driving adapter for the out-of-process test-control
// plane served under the reserved /_mock prefix (direct mint, enqueue behavior,
// capture/inspect). It is empty in the skeleton slice; the control endpoints
// land in Slice 5.
package controlapi
