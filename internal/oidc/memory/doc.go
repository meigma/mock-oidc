// Package memory is the driven adapter that implements the core's in-memory
// stores (keys, codes, refresh tokens, captured requests). It is empty in the
// skeleton slice; the store implementations land in Slice 1+.
package memory
